# Optional. Most data sources used in this project don't require a key.
# Only set this if you switch to fredapi instead of pandas-datareader.
FRED_API_KEY=

# Optional. For the streamlit deploy, point at a non-default DuckDB path.
LSR_DB_PATH=data/lsr.duckdb
